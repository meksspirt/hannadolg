# 📊 Анализатор долгов (React Edition)

Современное web-приложение для отслеживания долговых обязательств. Теперь на **React**, с использованием **Vite**, **Chart.js** и **Supabase**.

## ✨ Особенности
- **React + Hooks**: Быстрый и отзывчивый интерфейс.
- **Supabase Cloud**: Все данные хранятся в облаке PostgreSQL.
- **Авто-синхронизация**: Загружаете CSV — данные улетают в базу.
- **Умная аналитика**: Прогнозы погашения и статистика эффективности.
- **Modern UI**: Поддержка темной темы, пагинация и плавная анимация иконок (Lucide).

## 🚀 Как запустить локально

1. **Установите зависимости**:
   ```bash
   npm install
   ```

2. **Настройте базу данных**:
   - Создайте проект в [Supabase](https://supabase.com).
   - Выполните SQL для создания таблицы (см. ниже).
   - Создайте файл `.env` в корне и добавьте:
     ```env
     DATABASE_URL=ваша_строка_подключения
     ```

3. **Запустите сервер разработки**:
   ```bash
   npm run dev
   ```

## 🛠 SQL для Supabase

```sql
CREATE TABLE IF NOT EXISTS transactions (
    id SERIAL PRIMARY KEY,
    date DATE NOT NULL,
    category_name VARCHAR(255),
    payee VARCHAR(255),
    comment TEXT,
    outcome_account_name VARCHAR(255),
    outcome NUMERIC(10, 2),
    outcome_currency VARCHAR(10),
    income_account_name VARCHAR(255),
    income NUMERIC(10, 2),
    income_currency VARCHAR(10),
    created_date TIMESTAMP NOT NULL UNIQUE,
    changed_date TIMESTAMP,
    raw_line TEXT
);
```

## 🌍 Деплой на Vercel
1. Привяжите репозиторий GitHub к Vercel.
2. Добавьте Environment Variable `DATABASE_URL` в настройках Vercel.
3. Готово! Любой пуш в `main` автоматически обновит сайт.
